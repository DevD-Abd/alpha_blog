<%# Create new category page %>
<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm border-0">
        <div class="card-body">
          <h1 class="mb-4 text-center">Create a New Category</h1>
          
          <%= render 'shared/errors', obj: @category %>
          
          <%= form_with(model: @category, local: true, data: { turbo: false }) do |form| %>
            <div class="mb-3">
              <%= form.label :name, class: "form-label fw-semibold" %>
              <%= form.text_field :name, class: "form-control", placeholder: "Enter category name" %>
            </div>
            
            <div class="d-flex justify-content-end gap-2">
              <%= form.submit "Create Category", class: "btn btn-outline-info px-4" %>
              <%= link_to "Back to Categories", categories_path, class: "btn btn-outline-secondary px-4" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
