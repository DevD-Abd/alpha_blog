  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm border-bottom">
  <div class="container">
    <%= link_to "Alpha Blog", root_path, class: "navbar-brand fw-bold text-primary fs-3", id: "logo"%>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Articles", articles_path, class: "nav-link fw-semibold" %>
        </li>
        <li class="nav-item">
          <%= link_to "Bloggers", users_path, class: "nav-link fw-semibold" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle fw-semibold" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Categories
          </a>
          <ul class="dropdown-menu shadow-sm border-0">
            <%= link_to "All Categories", categories_path, class: "dropdown-item" %>
            <% if logged_in? && current_user.admin? %>
              <li><hr class="dropdown-divider"></li>
              <%= link_to "Create New Category", new_category_path, class: "dropdown-item text-primary" %>
            <% end %>
          </ul>
        </li>
        <li class="nav-item">
          <%= link_to "About", about_path, class: "nav-link fw-semibold" %>
        </li>
      </ul>
      
      <ul class="navbar-nav ms-auto">
        <% if logged_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <% if current_user.admin? %>
                <i class="bi bi-shield-check text-warning me-1"></i>
                <span class="fw-semibold"><%= current_user.username.capitalize %></span>
                <small class="text-muted ms-1">(Admin)</small>
              <% else %>
                <i class="bi bi-person-circle me-1"></i>
                <span class="fw-semibold"><%= current_user.username.capitalize %></span>
              <% end %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0">
              <%= link_to "View Profile", user_path(current_user), class: "dropdown-item" %>
              <%= link_to "Edit Profile", edit_user_path(current_user), class: "dropdown-item" %>
              <li><hr class="dropdown-divider"></li>
              <%= button_to "Logout", logout_path, method: :delete, class: "dropdown-item text-danger" %>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Log In", login_path, class: "nav-link fw-semibold" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", signup_path, class: "btn btn-primary ms-2 px-3" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
